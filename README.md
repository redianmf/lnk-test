# lnk-test

# DEV

Create db name "lnk" on localhost

```bash
cd client
npm install
npm start

cd server
cp .env.example .env
npm install
npm start

cd admin-laravel
composer install
php artisan serve

```

# FLOW
Register -> Login